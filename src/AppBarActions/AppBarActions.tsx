import styled from 'styled-components';

/**
 * 应用栏可操作区域
 */
const AppBarActions = styled.div.attrs(() => ({
  className: 'sinoui-app-bar--actions',
}))`
  display: flex;
  align-items: center;
`;

export default AppBarActions;
