/**
 * 日期单元格尺寸
 */
export default interface DateCellRect {
  /**
   * 宽度。此宽度包含内边距、边框尺寸。
   */
  width: number;
  /**
   * 高度。此高度包含内边距、边框尺寸。
   */
  height: number;
  /**
   * 内边距
   */
  padding: number;
}
