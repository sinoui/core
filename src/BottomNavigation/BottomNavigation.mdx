# BottomNavigation 底部导航栏

使用底部导航栏，您可以在应用程序的主要导航项之间跳转。

底部导航栏在屏幕下方显示三到五个导航项。 每一个导航项都由一个图标和一个可选文本标签表示。 当点击底部导航图标时，用户被切换到该图标关联的目标页面顶部。

## 初步认知

- [material-ui/BottomNavigation-组件](https://material-ui.com/zh/components/bottom-navigation/)
- [material-web-components/BottomNavigation demo](https://material.io/components/bottom-navigation/#specs)

## 用法

```tst
import BottomNavigation from '@sinoui/core/BottomNavigation';
import BottomNavigationAction from '@sinoui/core/BottomNavigationAction';
```

基本用法

```tsx
<BottomNavigation value={value} onChange={handleChange}>
  <BottomNavigationAction label="Recents" value="recents" icon={<MdRestore />} />
  <BottomNavigationAction label="Favorites" value="favorites" icon={<MdFavorite />} />
  <BottomNavigationAction label="Nearby" value="nearby" icon={<MdLocationOn />} />
  <BottomNavigationAction label="Folder" value="folder" icon={<MdFolder />} />
</BottomNavigation>
```

## 特性

- 最大宽度168px,最小宽度80px;

- 图标大小24;

- 有无label的padding值不同;

## BottomNavigation组件属性

| 属性名称 | 默认值      | 属性描述                                                 |
| -------- | ----------- | -------------------------------------------------------- |
| children |             | 子元素                                             |
| showLabels |             | 是否显示所有标签名称                             |
| onChange |             | 值变化的回调函数                            |
| value |                | value值                            |
| color |                | 指定背景颜色                            |
| className |             | 添加自定义类型                                             |

## BottomNavigationAction组件属性

| 属性名称 | 默认值      | 属性描述                                                 |
| -------- | ----------- | -------------------------------------------------------- |
| showLabel |             | 是否显示标签名称                             |
| value     |               | value值                             |

### API 文档

描述 BottomNavigation 组件。
