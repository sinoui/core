---
id: navigationRail
title: NavigationRail 垂直导航栏
---

使用垂直导航栏，您可以在应用程序的主要导航项之间跳转。

垂直导航栏在屏幕下方显示三到五个导航项。 每一个导航项都由一个图标和一个可选文本标签表示。 当点击底部导航图标时，用户被切换到该图标关联的目标页面顶部。

import NavigationRail from '@sinoui/core/NavigationRail';
import NavigationRailAction from '@sinoui/core/NavigationRailAction';
import { MdRestore, MdFavorite, MdLocationOn, MdFolder } from 'react-icons/md';
import { ThemeProvider } from 'styled-components';
import { defaultTheme } from '@sinoui/theme';
import NavigationRailDemo from './navigation-rail/NavigationRailDemo';
import NavigationRailBadge from './navigation-rail/NavigationRailBadge';

## NavigationRail

## 基本使用

```tsx
import NavigationRail from '@sinoui/core/NavigationRail';
import NavigationRailAction from '@sinoui/core/NavigationRailAction';
import Fab from '@sinoui/core/Fab';
import Add from '@sinoui/icons/Add';

function NavigationRailDemo() {
  const [val, setVal] = useState('');

  const Div = styled.div`
    position: relative;
    height: 660px;
  `;

  const NavigationRailWrapper = styled(NavigationRail)`
    padding-top: 72px;
  `;

  return (
    <ThemeProvider theme={defaultTheme}>
      <Div>
        <NavigationRailWrapper
          onChange={(_e: React.FormEvent<HTMLDivElement>, value: string) =>
            setVal(value)
          }
          value={val}
        >
          <Fab mini>
            <Add />
          </Fab>
          <NavigationRailAction
            label="Recents"
            value="recents"
            icon={<MdRestore />}
          />
          <NavigationRailAction
            label="Favorites"
            value="favorites"
            icon={<MdFavorite />}
          />
          <NavigationRailAction
            label="Nearby"
            value="nearby"
            icon={<MdLocationOn />}
          />
          <NavigationRailAction
            label="Folder"
            value="folder"
            icon={<MdFolder />}
          />
        </NavigationRailWrapper>
      </Div>
    </ThemeProvider>
  );
}
```

效果如下：

<NavigationRailDemo />

## 设置隐藏所有标签

```tsx
import NavigationRail from '@sinoui/core/NavigationRail';
import NavigationRailAction from '@sinoui/core/NavigationRailAction';

function NavigationRailDemo() {
  const [val, setVal] = useState('');

  const Div = styled.div`
    position: relative;
    height: 660px;
  `;

  const NavigationRailWrapper = styled(NavigationRail)`
    padding-top: 72px;
  `;

  return (
    <ThemeProvider theme={defaultTheme}>
      <Div>
        <NavigationRailWrapper
          onChange={(_e: React.FormEvent<HTMLDivElement>, value: string) =>
            setVal(value)
          }
          value={val}
          showLabels={false}
        >
          <NavigationRailAction
            label="Recents"
            value="recents"
            icon={<MdRestore />}
          />
          <NavigationRailAction
            label="Favorites"
            value="favorites"
            icon={<MdFavorite />}
          />
          <NavigationRailAction
            label="Nearby"
            value="nearby"
            icon={<MdLocationOn />}
          />
          <NavigationRailAction
            label="Folder"
            value="folder"
            icon={<MdFolder />}
          />
        </NavigationRailWrapper>
      </Div>
    </ThemeProvider>
  );
}
```

效果如下：

<NavigationRailDemo showLabels={false} />

## 与 Badge 结合使用

```tsx
import NavigationRail from '@sinoui/core/NavigationRail';
import NavigationRailAction from '@sinoui/core/NavigationRailAction';

function NavigationRailDemo() {
  const [val, setVal] = useState('');

  const Div = styled.div`
    position: relative;
    height: 660px;
  `;

  const NavigationRailWrapper = styled(NavigationRail)`
    padding-top: 72px;
  `;

  return (
    <ThemeProvider theme={defaultTheme}>
      <Div>
        <NavigationRailWrapper
          onChange={(_e: React.FormEvent<HTMLDivElement>, value: string) =>
            setVal(value)
          }
          value={val}
        >
          <NavigationRailAction
            label="Recents"
            value="recents"
            icon={
              <Badge count={8} dot>
                <MdRestore />
              </Badge>
            }
          />
          <NavigationRailAction
            label="Favorites"
            value="favorites"
            icon={<MdFavorite />}
          />
          <NavigationRailAction
            label="Nearby"
            value="nearby"
            icon={
              <Badge count={88}>
                <MdLocationOn />
              </Badge>
            }
          />
          <NavigationRailAction
            label="Folder"
            value="folder"
            icon={
              <Badge count={888}>
                <MdFolder />
              </Badge>
            }
          />
        </NavigationRailWrapper>
      </Div>
    </ThemeProvider>
  );
}
```

效果如下：

<NavigationRailBadge />

## 设置布局显示

### 居中显示

```tsx
import NavigationRail from '@sinoui/core/NavigationRail';
import NavigationRailAction from '@sinoui/core/NavigationRailAction';
import Fab from '@sinoui/core/Fab';
import Add from '@sinoui/icons/Add';

function NavigationRailDemo() {
  const [val, setVal] = useState('');

  const Div = styled.div`
    position: relative;
    height: 660px;
  `;

  const NavigationRailWrapper = styled(NavigationRail)`
    padding-top: 72px;
  `;

  return (
    <ThemeProvider theme={defaultTheme}>
      <Div>
        <NavigationRailWrapper
          onChange={(_e: React.FormEvent<HTMLDivElement>, value: string) =>
            setVal(value)
          }
          value={val}
          align="center"
        >
          <Fab>
            <Add />
          </Fab>
          <NavigationRailAction
            label="Recents"
            value="recents"
            icon={<MdRestore />}
          />
          <NavigationRailAction
            label="Favorites"
            value="favorites"
            icon={<MdFavorite />}
          />
          <NavigationRailAction
            label="Nearby"
            value="nearby"
            icon={<MdLocationOn />}
          />
          <NavigationRailAction
            label="Folder"
            value="folder"
            icon={<MdFolder />}
          />
        </NavigationRailWrapper>
      </Div>
    </ThemeProvider>
  );
}
```

效果如下：

<NavigationRailDemo align="center" />

### 从下向上显示

```tsx
import NavigationRail from '@sinoui/core/NavigationRail';
import NavigationRailAction from '@sinoui/core/NavigationRailAction';
import Fab from '@sinoui/core/Fab';
import Add from '@sinoui/icons/Add';

function NavigationRailDemo() {
  const [val, setVal] = useState('');

  const Div = styled.div`
    position: relative;
    height: 660px;
  `;

  const NavigationRailWrapper = styled(NavigationRail)`
    padding-top: 72px;
  `;

  return (
    <ThemeProvider theme={defaultTheme}>
      <Div>
        <NavigationRailWrapper
          onChange={(_e: React.FormEvent<HTMLDivElement>, value: string) =>
            setVal(value)
          }
          value={val}
          align="end"
        >
          <Fab>
            <Add />
          </Fab>
          <NavigationRailAction
            label="Recents"
            value="recents"
            icon={<MdRestore />}
          />
          <NavigationRailAction
            label="Favorites"
            value="favorites"
            icon={<MdFavorite />}
          />
          <NavigationRailAction
            label="Nearby"
            value="nearby"
            icon={<MdLocationOn />}
          />
          <NavigationRailAction
            label="Folder"
            value="folder"
            icon={<MdFolder />}
          />
        </NavigationRailWrapper>
      </Div>
    </ThemeProvider>
  );
}
```

效果如下：

<NavigationRailDemo align="end" />

## API

查看[NavigationRail 组件 API](/docs/components-apis/NavigationRail)
