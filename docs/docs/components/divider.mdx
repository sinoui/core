---
id: divider
title: Divider 水平分隔线
---

import CardStoryLayout from './CardStoryLayout';
import Divider from '@sinoui/core/Divider';
import {
  VertivalFullDivider,
  HorizontalDivider,
} from '../../../stories/dividerDemo';

## 水平分隔线(默认)

代码：

```tsx
<Wrapper>
  <ul>
    <li>item1</li>
    <Divider as="li" />
    <li>item2</li>
    <Divider as="li" />
    <li>item3</li>
    <Divider as="li" />
    <li>item4</li>
  </ul>
</Wrapper>
```

运行效果：

<CardStoryLayout>
  <HorizontalDivider />
</CardStoryLayout>

### 左边距为 72px 的水平分隔线

通过 marginLeft 属性设置左边距为 72px 的水平分隔线

代码：

```tsx
<Wrapper>
  <ul>
    <li>item1</li>
    <Divider as="li" marginLeft={72} />
    <li>item2</li>
    <Divider as="li" marginLeft={72} />
    <li>item3</li>
    <Divider as="li" marginLeft={72} />
    <li>item4</li>
  </ul>
</Wrapper>
```

运行效果：

<CardStoryLayout>
  <HorizontalDivider marginLeft={72} />
</CardStoryLayout>

### 右边距为 72px 的水平分隔线

通过 marginRight 属性设置右边距为 72px 的水平分隔线

代码：

```tsx
<Wrapper>
  <ul>
    <li>item1</li>
    <Divider as="li" marginRight={72} />
    <li>item2</li>
    <Divider as="li" marginRight={72} />
    <li>item3</li>
    <Divider as="li" marginRight={72} />
    <li>item4</li>
  </ul>
</Wrapper>
```

运行效果：

<CardStoryLayout>
  <HorizontalDivider marginRight={72} />
</CardStoryLayout>

## 水平居中分隔线(middle)

水平居中分隔线默认为 16px 的水平间距

代码

```tsx
<Wrapper>
  <ul>
    <li>item1</li>
    <Divider as="li" middle />
    <li>item2</li>
    <Divider as="li" middle />
    <li>item3</li>
    <Divider as="li" middle />
    <li>item4</li>
  </ul>
</Wrapper>
```

运行效果：

<CardStoryLayout>
  <HorizontalDivider middle />
</CardStoryLayout>

### 水平间距为 48px 的水平居中分隔线

通过 marginHorizontal 属性设置水平间距为 48px 的水平分隔线

代码

```tsx
<Wrapper>
  <ul>
    <li>item1</li>
    <Divider as="li" middle marginHorizontal={48} />
    <li>item2</li>
    <Divider as="li" middle marginHorizontal={48} />
    <li>item3</li>
    <Divider as="li" middle marginHorizontal={48} />
    <li>item4</li>
  </ul>
</Wrapper>
```

运行效果：

<CardStoryLayout>
  <HorizontalDivider middle marginHorizontal={48} />
</CardStoryLayout>

## 水平插入分隔线(inset)

水平插入分隔线默认为 72px 的左边距

代码

```tsx
<Wrapper>
  <ul>
    <li>item1</li>
    <Divider as="li" inset />
    <li>item2</li>
    <Divider as="li" inset />
    <li>item3</li>
    <Divider as="li" inset />
    <li>item4</li>
  </ul>
</Wrapper>
```

运行效果：

<CardStoryLayout>
  <HorizontalDivider inset />
</CardStoryLayout>

### 左边距为 108px 的水平插入分隔线

通过 marginVertical 属性设置左边距为 108px 的水平插入分隔线
水平插入分隔线默认为 108px 的左边距

代码

```tsx
<Wrapper>
  <ul>
    <li>item1</li>
    <Divider as="li" inset />
    <li>item2</li>
    <Divider as="li" inset />
    <li>item3</li>
    <Divider as="li" inset />
    <li>item4</li>
  </ul>
</Wrapper>
```

运行效果：

<CardStoryLayout>
  <HorizontalDivider inset marginLeft={108} />
</CardStoryLayout>

## 垂直分隔线

代码

```tsx
import React from 'react';
import styled from 'styled-components';
import Divider from '@sinoui/core/Divider';
import { MdSubject, MdToc } from 'react-icons/md';

const Wrapper = styled.div`
  display: flex;
  font-size: 24px;
  border: 1px solid ${(props) => props.theme.palette.divider};

  & > svg {
    margin: 8px;
  }
`;
export default function Demo() {
  return (
    <Wrapper>
      <MdSubject />
      <Divider vertical flexItem />
      <MdToc />
    </Wrapper>
  );
}
```

运行效果：

<CardStoryLayout>
  <VertivalFullDivider />
</CardStoryLayout>

### 垂直边距为 8px 的垂直分隔线

通过 marginVertical 属性设置垂直边距为 8px 的垂直分隔线

代码

```tsx
<Wrapper>
  <MdSubject />
  <Divider vertical flexItem marginVertical={8} />
  <MdToc />
</Wrapper>
```

运行效果：

<CardStoryLayout>
  <VertivalFullDivider marginVertical={8} />
</CardStoryLayout>
