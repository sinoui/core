---
id: checkboxGroup
title: CheckboxGroup 复选框组
---

CheckboxGroup 复选框组组件。

import { useCallback } from 'react';
import CheckboxGroup from '@sinoui/core/CheckboxGroup';
import Checkbox from '@sinoui/core/Checkbox';
import { ThemeProvider } from 'styled-components';
import { defaultTheme } from '@sinoui/theme';
import CheckboxGroupDemo from './checkbox-demos/CheckboxGroupDemo';

## CheckboxGroup

## 基本使用

```tsx
import CheckboxGroup from '@sinoui/core/CheckboxGroup';

function CheckboxGroupDemo() {
  const [value, setValue] = useState(['2', '3']);

  const onChange = (event: React.ChangeEvent<HTMLInputElement | any>) => {
    setValue(event.target.value);
  };

  return (
    <CheckboxGroup onChange={onChange} value={value}>
      <Checkbox value="1">复选框1</Checkbox>
      <Checkbox value="2">复选框2</Checkbox>
      <Checkbox value="3">复选框3</Checkbox>
      <Checkbox value="4">复选框4</Checkbox>
    </CheckboxGroup>
  );
}
```

效果如下：

<ThemeProvider theme={defaultTheme}>
  <CheckboxGroupDemo />
</ThemeProvider>

## 设置items属性

```tsx
import CheckboxGroup from '@sinoui/core/CheckboxGroup';

function CheckboxGroupDemo() {
  const [value, setValue] = useState(['2', '3']);

  const onChange = (event: React.ChangeEvent<HTMLInputElement | any>) => {
    setValue(event.target.value);
  };

  return (
    <CheckboxGroup 
    onChange={onChange} 
    value={value} 
    items={[
      <Checkbox value="1">复选框1</Checkbox>,
      <Checkbox value="2">复选框2</Checkbox>,
      <Checkbox value="3">复选框3</Checkbox>,
      <Checkbox value="4">复选框4</Checkbox>,
    ]} 
    />
  );
}
```

效果如下：

<ThemeProvider theme={defaultTheme}>
  <CheckboxGroupDemo
    items={[
      <Checkbox value="1">复选框1</Checkbox>,
      <Checkbox value="2">复选框2</Checkbox>,
      <Checkbox value="3">复选框3</Checkbox>,
      <Checkbox value="4">复选框4</Checkbox>,
    ]}
  /> 
</ThemeProvider>

## 设置是否支持全选

```tsx
import CheckboxGroup from '@sinoui/core/CheckboxGroup';

function CheckboxGroupDemo() {
  const [value, setValue] = useState(['2', '3']);

  const onChange = (event: React.ChangeEvent<HTMLInputElement | any>) => {
    setValue(event.target.value);
  };

  return (
    <CheckboxGroup enableSelectAll onChange={onChange} value={value}>
      <Checkbox value="1">复选框1</Checkbox>
      <Checkbox value="2">复选框2</Checkbox>
      <Checkbox value="3">复选框3</Checkbox>
      <Checkbox value="4">复选框4</Checkbox>
    </CheckboxGroup>
  );
}
```

效果如下：

<ThemeProvider theme={defaultTheme}>
  <CheckboxGroupDemo />  
</ThemeProvider>

## 设置不可用

```tsx
import CheckboxGroup from '@sinoui/core/CheckboxGroup';

function CheckboxGroupDemo() {
  return (
    <>
      不可用:
      <CheckboxGroup disabled value={['2']}>
        <Checkbox value="1">复选框1</Checkbox>
        <Checkbox value="2">复选框2</Checkbox>
      </CheckboxGroup>
      <br/>
      只读:
      <CheckboxGroup readOnly value={['2']}>
        <Checkbox value="1">复选框1</Checkbox>
        <Checkbox value="2">复选框2</Checkbox>
      </CheckboxGroup>
    </>
  );
}
```

效果如下：

<ThemeProvider theme={defaultTheme}>
  <>
    不可用:
    <CheckboxGroup disabled value={['2']}>
      <Checkbox value="1">复选框1</Checkbox>
      <Checkbox value="2">复选框2</Checkbox>
    </CheckboxGroup>
    <br/>
    只读:
    <CheckboxGroup readOnly value={['2']}>
      <Checkbox value="1">复选框1</Checkbox>
      <Checkbox value="2">复选框2</Checkbox>
    </CheckboxGroup>
  </> 
</ThemeProvider>

## 设置是否纵向显示

```tsx
import CheckboxGroup from '@sinoui/core/CheckboxGroup';

function CheckboxGroupDemo() {
  const [value, setValue] = useState(['2', '3']);

  const onChange = (event: React.ChangeEvent<HTMLInputElement | any>) => {
    setValue(event.target.value);
  };

  return (
    <CheckboxGroup column onChange={onChange} value={value}>
      <Checkbox value="1">复选框1</Checkbox>
      <Checkbox value="2">复选框2</Checkbox>
      <Checkbox value="3">复选框3</Checkbox>
      <Checkbox value="4">复选框4</Checkbox>
    </CheckboxGroup>
  );
}
```

效果如下：

<ThemeProvider theme={defaultTheme}>
  <CheckboxGroupDemo column />  
</ThemeProvider>

## 设置标题显示位置

```tsx
import CheckboxGroup from '@sinoui/core/CheckboxGroup';

function CheckboxGroupDemo() {
  const [value, setValue] = useState(['2', '3']);

  const onChange = (event: React.ChangeEvent<HTMLInputElement | any>) => {
    setValue(event.target.value);
  };

  return (
    <>
      标题显示在右侧：
      <CheckboxGroup onChange={onChange} value={value}>
        <Checkbox value="1">复选框1</Checkbox>
        <Checkbox value="2">复选框2</Checkbox>
      </CheckboxGroup>
      <br/>
      标题显示在左侧：
      <CheckboxGroup labelPosition="left" onChange={onChange} value={value}>
        <Checkbox value="1">复选框1</Checkbox>
        <Checkbox value="2">复选框2</Checkbox>
      </CheckboxGroup>
    </>
  );
}
```

效果如下：

<ThemeProvider theme={defaultTheme}>
  <>
    标题显示在右侧：
    <CheckboxGroupDemo />
    <br/>
    标题显示在左侧：
    <CheckboxGroupDemo labelPosition="left" />
  </>
</ThemeProvider>

## 设置网格对齐布局，默认3列

```tsx
import CheckboxGroup from '@sinoui/core/CheckboxGroup';

function CheckboxGroupDemo() {
  const [value, setValue] = useState(['2', '3']);

  const onChange = (event: React.ChangeEvent<HTMLInputElement | any>) => {
    setValue(event.target.value);
  };

  return (
    <>
      默认：
      <CheckboxGroup gridLayout onChange={onChange} value={value}>
        <Checkbox value="1">复选框1</Checkbox>
        <Checkbox value="2">复选框2</Checkbox>
        <Checkbox value="3">复选框3</Checkbox>
        <Checkbox value="4">复选框4</Checkbox>
        <Checkbox value="5">复选框5</Checkbox>
      </CheckboxGroup>
      <br/>
      设置colums为4：
      <CheckboxGroup gridLayout columns={4} onChange={onChange} value={value}>
        <Checkbox value="1">复选框1</Checkbox>
        <Checkbox value="2">复选框2</Checkbox>
        <Checkbox value="3">复选框3</Checkbox>
        <Checkbox value="4">复选框4</Checkbox>
        <Checkbox value="5">复选框5</Checkbox>
      </CheckboxGroup>
    </>
  );
}
```

效果如下：

<ThemeProvider theme={defaultTheme}>
  <>
    默认：
    <CheckboxGroupDemo gridLayout />
    <br/>
    设置colums为4：
    <CheckboxGroupDemo gridLayout columns={4} />
  </>
</ThemeProvider>

## API

查看[CheckboxGroup 组件 API](/docs/components-apis/checkboxGroup)
